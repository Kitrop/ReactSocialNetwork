
let initialState = {
    friendsData: [
        {username: 'Evgeniy', id: 1},{username: 'Svetlana', id: 2},{username: 'Dmitryi', id: 3},
    ],
};

const friendsReducer = (state = initialState, action) => {
    return state;
}

export default friendsReducer;